import { Text, View } from 'react-native';

export default function ToDo() {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: 'lightblue',
      }}>
      <Text className="text-xl font-spacemono">ToDo</Text>
    </View>
  );
}
